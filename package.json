{
  "name": "iso-fns",
  "version": "0.0.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "yarn lint && c8 -r none beartest 'dist/**/*.test.js'",
    "cc": "c8 report --all -r html -r text",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "@types/dateformat": "^3.0.1",
    "c8": "^7.7.3",
    "dateformat": "^4.5.1"
  },
  "devDependencies": {
    "all-contributors-cli": "^6.20.0",
    "beartest-js": "^3.1.1",
    "expect": "^27.0.6",
    "prettier": "^2.3.2",
    "typescript": "^4.3.2"
  }
}
