{
  "name": "iso-fns",
  "version": "0.0.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "yarn lint && beartest 'dist/**/*.test.js'",
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "code-coverage": "c8 --check-coverage --lines 95 --all yarn test"
  },
  "dependencies": {
    "@types/dateformat": "^3.0.1",
    "c8": "^7.7.3",
    "dateformat": "^4.5.1"
  },
  "devDependencies": {
    "all-contributors-cli": "^6.20.0",
    "beartest-js": "^3.1.1",
    "expect": "^27.0.6",
    "prettier": "^2.3.2",
    "typescript": "^4.3.2"
  }
}
